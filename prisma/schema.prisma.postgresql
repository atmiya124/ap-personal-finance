// PostgreSQL version - Use this for production
// To switch: rename this file to schema.prisma (backup the old one first)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ... rest of the schema remains the same ...

